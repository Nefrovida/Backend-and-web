# Frontend Vite env example (copy to frontend/.env for local development)
# VITE_ prefixed variables are exposed to client code

# Base URL for backend API. Should include /api if the frontend expects it (eg: https://www.snefrovidaac.com/api)
VITE_APP_API_URL=http://localhost:3001/api#https://www.snefrovidaac.com/api

# Origin (frontend) used for CORS configuration or build-time values
VITE_APP_ORIGIN=http://localhost:3001#https://www.snefrovidaac.com

# Optional: Proxy target for Vite dev server (backend origin, without /api)
VITE_APP_PROXY_TARGET=http://localhost:3001#https://www.snefrovidaac.com
